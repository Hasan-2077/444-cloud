AWS credentials and GitHub tokens should never be stored in a repository because anyone who gains access to them could use them to access your accounts and sensitive information. This risk exists even with private repositories since they can still be leaked, shared, or hacked. Instead, credentials should be stored securely using environment variables, secret managers, or other protected methods.
